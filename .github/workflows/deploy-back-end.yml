name: Deploy Docker Compose

on:
  push:
    branches:
      - deploy-back-end

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Docker
        uses: KengoTODA/actions-setup-docker-compose@v1
        with:
          version: "2.14.2"

      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: "ACR Login"
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.ACR_LOGIN_SERVER }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and Push Docker images
        # TODO: Use ${{ github.sha }} for tags
        run: |
          docker-compose build 
            ${{ secrets.ACR_LOGIN_SERVER }}/wallet-server
            ${{ secrets.ACR_LOGIN_SERVER }}/miner-1 
            ${{ secrets.ACR_LOGIN_SERVER }}/miner-2 
            ${{ secrets.ACR_LOGIN_SERVER }}/miner-3
            
          docker-compose push 
            ${{ secrets.ACR_LOGIN_SERVER }}/wallet-server 
            ${{ secrets.ACR_LOGIN_SERVER }}/miner-1 
            ${{ secrets.ACR_LOGIN_SERVER }}/miner-2 
            ${{ secrets.ACR_LOGIN_SERVER }}/miner-3

      # - name: Deploy to Azure Container Instances
      #   uses: "azure/aci-deploy@v1"
      #   with:
      #     name: GoBlockchain
      #     resource-group: Go_Blockchain
      #     location: northeurope
      #     dns-name-label: Go-Blockchain
      #     ports: |
      #       5000:5000
      #       5001:5001
      #       5002:5002
      #       5003:5003
      #     image: |
      #       ${{ secrets.ACR_LOGIN_SERVER }}/dev/wallet-server:latest
      #       ${{ secrets.ACR_LOGIN_SERVER }}/dev/miner-1:latest
      #       ${{ secrets.ACR_LOGIN_SERVER }}/dev/miner-2:latest
      #       ${{ secrets.ACR_LOGIN_SERVER }}/dev/miner-:latest
      #     cpu: 1
      #     ip-address: Public
      #     memory: 1.5
      #     os-type: Linux
      #     protocol: TCP
      #     restart-policy: Always
